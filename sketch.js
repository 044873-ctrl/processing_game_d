let cols=10;let rows=20;let cell=30;let board=[];let currentPiece=null;let frameCounter=0;let dropInterval=30;let gameOver=false;let score=0;let colorList=["red","blue","yellow","green"];let dxArr=[1,0,-1,0];let dyArr=[0,1,0,-1];function initBoard(){board=[];for(let r=0;r<rows;r++){let row=[];for(let c=0;c<cols;c++){row.push(null);}board.push(row);}}function getBlocks(piece){let blocks=[];if(piece===null){return blocks;}let x=piece.x;let y=piece.y;let rot=piece.rot;blocks.push({x:x
