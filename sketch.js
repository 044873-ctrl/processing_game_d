let balls=[];let spawnTimer=0;let spawnInterval=90;let score=0;let strikes=0;let maxStrikes=3;let ballRadius=12;let pitchSpeedBase=3;let pitchSpeedIncrease=0.01;let frameCounter=0;let gameOver=false;let bat={x:200,y:520,w:100,h:14,reachX:90,reachY:40,angle:0,swinging:false,swingTimer:0,swingDuration:12};function spawnBall(){let sx=Math.floor(random(60,340));let svx=random(-1,1);let svy=pitchSpeedBase+random(0,1)+frameCounter*pitchSpeedIncrease;let b={x:sx,y:-20,vx:svx,vy:svy,r:ballRadius};balls.push(b);}function startSwing(){if(gameOver){return;}if(!bat.swinging){bat.swinging=true;bat.swingTimer=0;}}function updateBat(){if(bat.swinging){bat.swingTimer+=1;let t=bat.swingTimer;let d=bat.swingDuration;bat.angle=map(t,0,d,-PI/4,PI/6);if(bat.swingTimer>bat.swingDuration){bat.swinging=false;bat.swingTimer=0;bat.angle=0;}}if(keyIsDown(LEFT_ARROW)){bat.x=max(bat.w/2,bat.x-6);}if(keyIsDown(RIGHT_ARROW)){bat.x=min(400-bat.w/2,bat.x+6);}if(mouseIsPressed){if(!bat.swinging){startSwing();}}}function detectHit(b){if(!bat.swinging){return false;}if(b.y>bat.y+bat.reachY || b.y<bat.y-b.reachY-40){return false;}let dx=b.x-bat.x;let dy=b.y-bat.y;let distSq=dx*dx+dy*dy;let maxReach=(bat.reachX+Math.max(b.r,4));if(distSq>maxReach*maxReach){return false;}let hitWindowStart=Math.floor(bat.swingDuration*0.25);let hitWindowEnd=Math.floor(bat.swingDuration*0.9);if(bat.swingTimer<hitWindowStart || bat.swingTimer>hitWindowEnd){return false;}return true;}function updateBalls(){for(let i=balls.length-1;i>=0;i--){let b=balls[i];b.x+=b.vx;b.y+=b.vy;b.vy+=0.02;let hit=detectHit(b);if(hit){score+=1;balls.splice(i,1);continue;}if(b.y-b.r>600){strikes+=1;balls.splice(i,1);if(strikes>=maxStrikes){gameOver=true;}continue;}}}function resetGame(){balls=[];spawnTimer=0;spawnInterval=90;score=0;strikes=0;frameCounter=0;gameOver=false;bat.x=200;bat.angle=0;bat.swinging=false;bat.swingTimer=0;}function setup(){createCanvas(400,600);textFont('monospace');}function draw(){background(30,130,80);frameCounter+=1;spawnTimer+=1;if(!gameOver){if(spawnTimer>=spawnInterval){spawnTimer=0;spawnInterval=max(30,Math.floor(spawnInterval*0.98));spawnBall();}updateBat();updateBalls();}fill(200);noStroke();ellipse(200,70,80,80);fill(80);ellipse(200,70,16,16);fill(255);for(let i=0;i<balls.length;i++){let b=balls[i];fill(255,200,0);ellipse(b.x,b.y,b.r*2,b.r*2);fill(200);}push();translate(bat.x,bat.y);rotate(bat.angle);fill(120);rectMode(CENTER);rect(0,0,bat.w,bat.h,6);pop();fill(255);textSize(18);text('Score: '+score,12,22);text('Strikes: '+strikes+'/'+maxStrikes,12,44);textSize(12);text('Use ← → to move, click or space to swing, r to restart',12,580);if(gameOver){fill(0,180,0);rect(40,200,320,180,10);fill(255);textSize(28);textAlign(CENTER,CENTER);text('Game Over',200,250);textSize(18);text('Score: '+score,200,290);text('Press r to restart',200,320);textAlign(LEFT,BASELINE);}function keyPressed(){if(key===' '){startSwing();}if(key==='r'||key==='R'){resetGame();}}function mousePressed(){startSwing();}}
