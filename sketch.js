let canvasW=800,canvasH=400;let balls=[];let pitcherX,requestPitchTimer,pitcherY;let batterX,batterY;let batY;let batAngle=0;let swinging=false;let swingTimer=0,swingDuration=12;let pitchReady=true;let pitchTimer=0,pitchDelay=60;let hits=0,strikes=0,ballsCount=0,outs=0;let pitchTypes=['fastball','slider'];let currentPitchType='';class Ball{constructor(type,x,y,vx,vy){this.type=type;this.x=x;this.y=y;this.vx=vx;this.vy=vy;this.size=14;this.hit=false}update(){this.x+=this.vx;this.y+=this.vy}draw(){fill(255,200,0);ellipse(this.x,this.y,this.size,this.size)}}function spawnPitch(){let typeIdx=floor(random(0,pitchTypes.length));currentPitchType=pitchTypes[typeIdx];let startX=pitcherX;let startY=pitcherY;let speedPxPerFrame=8;let vx=-speedPxPerFrame;let vy=0;if(currentPitchType==='slider'){let dir=random([-1,1]);vy=1.6*dir}let b=new Ball(currentPitchType,startX,startY,vx,vy);balls.push(b);pitchReady=false;pitchTimer=0}function resetCountsAfterOut(){strikes=0;ballsCount=0}function processBallHit(index){hits++;balls.splice(index,1)}function processBallMiss(index){strikes++;balls.splice(index,1);if(strikes>=3){outs++;resetCountsAfterOut()}}function setup(){createCanvas(canvasW,canvasH);pitcherX=canvasW-80;pitcherY=canvasH/2;batterX=120;batterY=canvasH/2;batY=batterY;textSize(16);rectMode(CENTER);ellipseMode(CENTER);noStroke()}function draw(){background(30,120,40);fill(200);rect(pitcherX,pitcherY,40,80);fill(120);rect(batterX,batterY,20,80);for(let i=balls.length-1;i>=0;i--){let b=balls[i];b.update();b.draw();if(b.x<0||b.x>canvasW||b.y<0||b.y>canvasH){balls.splice(i,1)}else{if(swinging&&swingTimer>2){let reach=80;let hitTop=batY-40;let hitBottom=batY+40;let hitLeft=batterX;let hitRight=batterX+reach;if(b.x>=hitLeft&&b.x<=hitRight&&b.y>=hitTop&&b.y<=hitBottom){processBallHit(i)}}else if(b.x<=batterX+10&&!swinging){processBallMiss(i)}}}if(pitchReady){pitchTimer++;if(pitchTimer>=pitchDelay){spawnPitch()}}else{if(balls.length===0){pitchReady=true}}if(keyIsDown(38)){batY-=4}if(keyIsDown(40)){batY+=4}batY=constrain(batY,40,canvasH-40);if(keyIsDown(32)&&!swinging){swinging=true;swingTimer=0}if(swinging){swingTimer++;let progress=swingTimer/swingDuration;batAngle=sin(progress*PI)*-PI/3;if(swingTimer>=swingDuration){swinging=false;batAngle=0}}push();translate(batterX,batY);rotate(batAngle);fill(180);rect(30,0,80,12);pop();fill(255);text('Hits: '+hits,10,20);text('Strikes: '+strikes,10,40);text('Outs: '+outs,10,60);text('Pitch: '+currentPitchType,canvasW-160,20)}
